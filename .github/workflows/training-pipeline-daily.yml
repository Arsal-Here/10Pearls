# =============================================================================
# Training Pipeline — Daily Scheduled Workflow
# =============================================================================
# Pulls features from Hopsworks, trains multiple models (XGBoost, RF, Ridge),
# evaluates performance, and registers the best model in the Model Registry.
#
# Schedule: Daily at 02:00 UTC
# Manual: Can also be triggered via workflow_dispatch
# =============================================================================

name: Training Pipeline (Daily)

on:
  schedule:
    # Run daily at 2:00 AM UTC (gives time for hourly features to accumulate)
    - cron: "0 2 * * *"
  workflow_dispatch:
    # Allow manual triggering from the Actions tab

env:
  PYTHON_VERSION: "3.11"

jobs:
  training-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      # --------------------------------------------------
      # 1. Checkout repository
      # --------------------------------------------------
      - name: Checkout code
        uses: actions/checkout@v4

      # --------------------------------------------------
      # 2. Set up Python
      # --------------------------------------------------
      - name: Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      # --------------------------------------------------
      # 3. Install dependencies
      # --------------------------------------------------
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # --------------------------------------------------
      # 4. Run training pipeline
      # --------------------------------------------------
      - name: Run Training Pipeline
        env:
          HOPSWORKS_API_KEY: ${{ secrets.HOPSWORKS_API_KEY }}
          HOPSWORKS_PROJECT: ${{ secrets.HOPSWORKS_PROJECT }}
        run: |
          python training_pipeline.py

      # --------------------------------------------------
      # 5. Report status
      # --------------------------------------------------
      - name: Pipeline status
        if: always()
        run: |
          echo "Training pipeline completed with status: ${{ job.status }}"
          echo "Triggered by: ${{ github.event_name }}"
          echo "Timestamp: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
